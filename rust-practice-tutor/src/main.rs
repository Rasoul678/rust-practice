fn main() {
    // parts::one::variables();
    // parts::two::ownership();
    // parts::three::functions();
    // parts::four::structures();
    parts::five::enumerations();
}

mod parts {
    pub mod five;
    // pub mod four;
    // pub mod one;
    // pub mod three;
    // pub mod two;
}
